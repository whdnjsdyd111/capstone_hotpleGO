<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/schema/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.HotpleMapper">

    <select id="readAddr" resultType="java.lang.Long">
        SELECT htId FROM hotple WHERE htAddr = #{str}
    </select>

    <insert id="insertBusn">
        INSERT INTO hotple (htId, busnNum, htAddr, htAddrDet, htZip, htCont, htTel, htImg, htLat, htLng, uCode, category)
        VALUES(seq_hot.NEXTVAL, #{busnNum}, #{htAddr}, #{htAddrDet}, #{htZip}, #{htCont}, #{htTel},
        #{htImg}, #{htLat}, #{htLng}, #{uCode}, #{category})
    </insert>
</mapper>